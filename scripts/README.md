# Chronicle Build Scripts

This folder contains the build tooling for the Community Garden Chronicle.

`build-chronicle.py` converts the Markdown issues in `content/chronicle/` into:

- the Chronicle landing page and archive
- individual issue pages
- the RSS feed used by HubSpot

Draft issues are excluded from the public RSS feed.
